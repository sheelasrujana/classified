<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="action_classifier_dashboard_cio" model="ir.actions.server">
        <field name="name">CIO</field>
        <field name="model_id" ref="model_sale_order"/>
        <field name="state">code</field>
        <field name="code">action = model.classifier_dashboard_cio()</field>
    </record>

    <record id="action_classifier_dashboard_scheduling" model="ir.actions.server">
        <field name="name">Scheduling</field>
        <field name="model_id" ref="model_sale_order"/>
        <field name="state">code</field>
        <field name="code">action = model.classifier_dashboard_scheduling()</field>
    </record>

    <record id="action_classifier_dashboard_waiting_for_approval" model="ir.actions.server">
        <field name="name">Waiting For Approval</field>
        <field name="model_id" ref="model_sale_order"/>
        <field name="state">code</field>
        <field name="code">action = model.classifier_dashboard_waiting_for_approval()</field>
    </record>

    <record id="action_classifier_dashboard_release_orders" model="ir.actions.server">
        <field name="name">Release Orders</field>
        <field name="model_id" ref="model_sale_order"/>
        <field name="state">code</field>
        <field name="code">action = model.classifier_dashboard_release_orders()</field>
    </record>

    <record id="action_classifier_dashboard_invoices" model="ir.actions.server">
        <field name="name">Invoices</field>
        <field name="model_id" ref="model_account_move"/>
        <field name="state">code</field>
        <field name="code">action = model.classifier_dashboard_invoices()</field>
    </record>

</odoo>